<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/static/primeflex.min.css" />
  <title><%= typeof title !== 'undefined' ? title : 'Bloc-notes' %></title>
  <style>
    body {
      font-family: system-ui, sans-serif;
      background: #f4f4f8;
      margin: 0;
      padding: 2rem;
    }
    h1 {
      text-align: center;
      color: #333;
    }
    .container {
      max-width: 600px;
      margin: 2rem auto;
      background: white;
      padding: 2rem;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0,0,0,0.05);
    }
    form {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }
    input[type="text"],
    textarea {
      padding: 0.8rem;
      border: 1px solid #ccc;
      border-radius: 6px;
      font-size: 1rem;
    }
    button {
      background: #4f46e5;
      color: white;
      padding: 0.8rem;
      border: none;
      border-radius: 6px;
      font-size: 1rem;
      cursor: pointer;
    }
    button:hover {
      background: #4338ca;
    }
    .btn-primary {
      background-color: #4f46e5;
      color: white;
    }

    .btn-primary:hover {
      background-color: #4338ca;
    }

    .btn-danger {
      background-color: #dc3545;
      color: white;
    }

    .btn-danger:hover {
      background-color: #a71d2a;
    }
    .note {
      border-bottom: 1px solid #eee;
      padding: 1rem 0;
    }
    .note:last-child {
      border: none;
    }
    a {
      text-decoration: none;
      color: #4f46e5;
    }
    a:hover {
      text-decoration: underline;
    }
    .divider {
      display: flex;
      flex-direction: row;
      justify-content: space-around;
    }
    .form-section {
      margin-bottom: 0;
    }
    .action-buttons {
      display: flex;
      gap: 1rem;
      margin-top: 1rem;
      justify-content: center;
    }
  </style>
</head>

